components:
    schemas:
        error:
            type: object
            properties:
                error:
                    type: string
                    description: description of the error
            example:
                error: 'There is a problem with the request. Try again.'
